######################
  Getting set up
######################

0) (Initial step after merge)

   Edit Dockerfile to copy the root of the website into the image

1) build the image

   sudo ./build.sh

2) run the image

   sudo docker run --name course-scheduler-ui -d -p 8080:80 course-scheduler/ui:0.0.1

3) view webpage

   http://localhost:8080